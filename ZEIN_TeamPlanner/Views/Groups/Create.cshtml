@model ZEIN_TeamPlanner.Models.CreateGroupDto

@{
    ViewData["Title"] = "Create Group";
}

<h2>Create New Group</h2>



<form asp-action="Create" method="post">
    <div class="form-group mb-3">
        <label asp-for="GroupName" class="form-label">Group Name</label>
        <input asp-for="GroupName" class="form-control" required />
        <span asp-validation-for="GroupName" class="text-danger"></span>
    </div>
    <div class="form-group mb-3">
        <label asp-for="Description" class="form-label">Description</label>
        <textarea asp-for="Description" class="form-control" rows="4"></textarea>
        <span asp-validation-for="Description" class="text-danger"></span>
    </div>
    @* <div class="form-group mb-3">
        <label asp-for="MemberIds" class="form-label">Add Members (Optional)</label>
        <select asp-for="MemberIds" class="form-select" multiple>
            @foreach (var user in ViewBag.Users)
            {
                <option value="@user.Id">@user.FullName</option>
            }
        </select>
        <span asp-validation-for="MemberIds" class="text-danger"></span>
    </div> *@
    <div class="form-group mb-3">
        <button type="submit" class="btn btn-primary">Create Group</button>
        <a asp-action="Index" asp-controller="Groups" class="btn btn-secondary">Cancel</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}