@model ZEIN_TeamPlanner.Models.TaskItem

@{
    ViewData["Title"] = "Delete Task";
}

<h2>Delete Task</h2>

<h3>Are you sure you want to delete this task?</h3>

<div class="card">
    <div class="card-body">
        <h5 class="card-title">@Model.Title</h5>
        <dl class="row">
            <dt class="col-sm-3">Group</dt>
            <dd class="col-sm-9">@(Model.Group?.GroupName ?? "Unknown")</dd>
            <dt class="col-sm-3">Status</dt>
            <dd class="col-sm-9">@Model.Status</dd>
            <dt class="col-sm-3">Deadline</dt>
            <dd class="col-sm-9">@(Model.Deadline.HasValue? Model.Deadline.Value.ToString("dd MMM yyyy HH:mm") : "None")</dd>
            <dt class="col-sm-3">Assigned To</dt>
            <dd class="col-sm-9">@(Model.AssignedToUser?.FullName ?? "Unassigned")</dd>
        </dl>

        <form asp-action="Delete" method="post">
            <input type="hidden" asp-for="TaskItemId" />
            <button type="submit" class="btn btn-danger">Delete</button>
            <a asp-action="Index" asp-route-groupId="@Model.GroupId" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
</div>